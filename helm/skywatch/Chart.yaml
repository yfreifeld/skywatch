apiVersion: v2
name: skywatch
description: SkyWatch distributed weather pipeline (Frontend + Worker + RabbitMQ)
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: rabbitmq
    version: "16.x.x"
    repository: "oci://registry-1.docker.io/bitnamicharts"
    alias: rabbitmq
